name: build-assets

on:
  push:
    branches:
      - main
      - master
    paths-ignore:
      - 'public/build/**'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build-assets-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    if: github.actor != 'github-actions[bot]'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref_name }}

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          tools: composer:v2

      - name: Install PHP dependencies
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: Verify Laravel autoload and Wayfinder command
        run: |
          test -f vendor/autoload.php
          php artisan wayfinder:generate --with-form

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build frontend assets
        run: NODE_ENV=production npm run build

      - name: Verify built assets
        run: |
          test -f public/build/manifest.json
          grep -q '"resources/js/app.ts"' public/build/manifest.json

      - name: Commit built assets
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Move the fresh build aside so the working tree is clean for rebase
          cp -r public/build /tmp/fresh-build

          # Restore public/build to whatever git last knew (tracked files only)
          git checkout -- public/build 2>/dev/null || true
          # Remove any untracked new-hash files the build created
          git clean -fd public/build

          # Now rebase is safe — no unstaged changes in public/build
          git pull --rebase origin "${GITHUB_REF_NAME}"

          # Put the freshly built assets back
          rm -rf public/build
          cp -r /tmp/fresh-build public/build

          git add -f public/build
          git diff --cached --quiet && exit 0
          git commit -m "chore(ci): build frontend assets [skip ci]"
          git push origin HEAD:"${GITHUB_REF_NAME}"
